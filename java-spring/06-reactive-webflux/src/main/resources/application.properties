# Реактивный H2 через R2DBC (статья 04): драйвер не блокирует поток на I/O.
spring.r2dbc.url=r2dbc:h2:mem:///products;DB_CLOSE_DELAY=-1

# Схему накатывает schema.sql при старте (у R2DBC нет ddl-auto).
spring.sql.init.mode=always
