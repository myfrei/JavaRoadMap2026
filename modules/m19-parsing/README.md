# Модуль 19 — Парсинг и компиляторы (код)

Запускаемый код-скелет модуля. Теория, уроки и критерии приёмки проекта —
в [`../../course/19-parsing.md`](../../course/19-parsing.md).

```bash
./gradlew :modules:m19-parsing:run     # запустить Main
./gradlew :modules:m19-parsing:test    # прогнать тесты
```
