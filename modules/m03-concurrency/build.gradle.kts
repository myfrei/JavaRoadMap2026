// Код-скелет модуля 03 — «Конкурентность».
// Java 25, JUnit 5 и application-плагин наследуются из корневого build.gradle.kts.
// Модуль-специфичные зависимости добавляй здесь, например:
//   dependencies { implementation(libs.jackson.databind) }
